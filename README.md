# Random Picker

Pick a random item from from a list

How to setup a development environment:

```bash
make setup
```

To run that dev env:

```bash
make dev
```

To build the project:

```bash
make
```

To lint the project:

```bash
make lint
```
